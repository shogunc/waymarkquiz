[build]
  command = "npm run build"
  publish = "dist"

# React Router uses client-side routing (BrowserRouter) — send every path to
# index.html so deep links like /join or /admin/quizzes/:id don't 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
